<%= form_for([@city, @employee]) do |f| %>

	<p>
		<%= f.label :name %><br>
		<%= f.text_area :name %>
	</p>
	<p>
		<%= f.label :bio %><br>
		<%= f.text_area :bio %>
	</p>
	<p>
		<%= f.label :image %><br>
		<%= f.text_area :image %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>

