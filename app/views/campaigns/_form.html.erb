<%= form_for([@city, @city.campaigns.build]) do |f| %>

	<p>
		<%= f.label :name %><br>
    	<%= f.text_area :name %>
	</p>
	<p>
		<%= f.label :description %><br>
    	<%= f.text_area :description %>
    </p>
   	<p>
   		<%= f.submit %>
   	</p>
<% end %>
